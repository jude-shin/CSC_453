#include <stdlib.h>
#include <stdio.h>
#include <unistd.h>

#define SBRK_CHUNKS 64

// NOTE: use multiples of 16 bytes

int main(int argc, char *argv[]) {
  // use sbrk to generate a buffer size
  // the return should be the beginning pointer where we can start allocating
  // we should use this as the header
  // 

  // use a linked list style data structure

  return 0;
}
