#ifndef STATUS 
#define STATUS 

/* Prints all philosophers' statuses on one line. */
void print_status_line(void);

/* Prints a line break of "=". */
void print_break_line(void);

/* Prints the associated ASCII character associated with a philosopher's index
   (ex. 0->'A', 1->'B'...). */
void print_name_line(void);

#endif
